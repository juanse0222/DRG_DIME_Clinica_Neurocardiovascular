# The raw cost RDS is excluded via data/.rscignore (auto-generated by
# prep_data.R) — rsconnect's ignore matching is literal-filename-only, per
# directory, so a "data/..." wildcard pattern here would silently never match.
# Run prep_data.R locally to generate pte_base.rds / une_base.rds / dt_base.rds
prep_data.R
